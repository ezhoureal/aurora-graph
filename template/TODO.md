# Template TODOs

- [x] Add support for `mask` source templates or filter auto-registered templates so nodes with required `mask` inputs are not surfaced before they can be wired.
- [x] Add editable UI support for structured parameter kinds such as `object`, `numberTuple`, `numberArray`, and `pointArray`
